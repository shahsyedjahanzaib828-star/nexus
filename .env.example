# Production origin used for canonical URLs, sitemap, Open Graph, and structured data.
# No trailing slash. Leave unset to use the current Vercel deployment URL.
# PUBLIC_SITE_URL=https://www.example.com
